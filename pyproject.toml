[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "thesis-p4rf"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["src", "src.p4gen", "src.training", "src.reporting"]
